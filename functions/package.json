{
  "name": "village-functions",
  "version": "1.0.0",
  "description": "Village Cloud Functions: matching engine and backend logic",
  "main": "lib/index.js",
  "type": "module",
  "scripts": {
    "test": "node --test src/matching.test.ts",
    "build": "tsc",
    "watch": "tsc --watch",
    "lint": "eslint src/ --fix",
    "deploy": "firebase deploy --only functions"
  },
  "keywords": [
    "village",
    "matching",
    "gale-shapley",
    "firebase"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.0.0"
  }
}
